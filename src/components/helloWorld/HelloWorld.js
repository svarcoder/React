/**
 * This is a React functional component that returns a div element with the text "HelloWorld".
 * @returns A React component that renders a `<div>` element with the text "HelloWorld".
 */
import React from "react";

const HelloWorld = () => {
  return <div>HelloWorld</div>;
};

export default HelloWorld;
