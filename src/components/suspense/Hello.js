/**
 * This is a React functional component that returns a div element with the text "Hello React".
 * @returns A React component that renders a `<div>` element with the text "Hello React".
 */
import React from "react";

const Hello = () => {
  return <div>Hello React</div>;
};

export default Hello;
